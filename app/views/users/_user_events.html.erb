<% if events.any? %>
  <ul class="list-unstyled">
    <% events.each do |event| %>
      <li><%= event.description %> (<%= link_to 'Details', event %>)</li>
    <% end %>
  </ul>
<% else %>
  No events
<% end %>
