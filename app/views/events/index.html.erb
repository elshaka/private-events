<section class="upcoming-events">
  <h2>Upcoming Events</h2>
  <%= render "table", events: @upcoming_events %>
</section>
<section class="past-events">
  <h2>Past Events</h2>
  <%= render "table", events: @prev_events %>
</section>
